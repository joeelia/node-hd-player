#!/usr/bin/python
# -*- coding: utf-8 -*-
from hosted import device, node, config, json, os, sys, requests, config, requests
import ibquery
ib = ibquery.InfoBeamerQuery()
s = ib.node('root/proof-of-play').io(raw=True)

my_dict = []
my_dict.append({'device_id' : config.metadata['device_id']})
print(my_dict)
my_dict.append(s.readline())
print(my_dict)
print s.event
